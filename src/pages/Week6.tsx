import React from "react";

const Week6: React.FC = () => {
    return (
        <div>
            <h2>Text Encryption via Stream Cipher and Steganography</h2>
            <p>Take two input messages. Encrypt message 2 using a stream
                cipher and then produce an output containing the first
                input with the second input hidden within it</p>
        </div>
    );
}

export default Week6;
