import React from "react";

const Home: React.FC = () => {
    return (
        <div>
            <h1 className={"header"}>Cryptography</h1>
            <p>Each page will contain the work relevant to that week.</p>
        </div>
    );
}

export default Home;
